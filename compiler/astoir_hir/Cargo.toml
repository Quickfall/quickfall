[package]
name = "astoir_hir"
version = "0.1.0"
edition = "2024"

[dependencies]
typing = { path = "../typing" }
compiler_utils = { path = "../compiler_utils" }
diagnostics = { path = "../diagnostics" }
