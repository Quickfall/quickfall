[package]
name = "ast"
version = "0.1.0"
edition = "2024"

[dependencies]
compiler_utils = { path = "../compiler_utils" }
compiler_typing = { path = "../compiler_typing" }
lexer = { path = "../lexer" }
