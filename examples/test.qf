func main(si32 myTestArg, si32 age) {
    var si32 test = test.abc().test;

	var si32 test2 = test.abs() >= test.def();

	if(test2 >= test) {
		var si32 age = 0;
	} else if(test2 == test) {
		var si32 age = 63;
	} else {
		var si32 age = 68;
	}
}